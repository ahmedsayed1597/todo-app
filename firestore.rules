rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // todos — each user can only read and write their own tasks
    match /users/{uid}/todos/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
